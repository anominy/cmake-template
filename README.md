# C++ Project

## CMake
### Init
Initializes this CMake project.
```shell
just init
```

### Clean
Cleans this CMake project.
```shell
just clean
```

### Build
Builds this CMake project.
```shell
just build
```

### Run
Runs this CMake project with provided arguments.
```shell
just run [*]
```
